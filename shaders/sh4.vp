#version 330

layout(location=0) in vec4 position;
layout(location=1) in vec4 color;

out vec4 vertexColor;

void main(void){

  vertexColor = color;
  // We will do the projections in the geometry shader.
  gl_Position = position;

}
